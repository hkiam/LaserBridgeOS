# The appliance keeps root login disabled (ADR 0002). Remote deployment still
# needs kexec and raw writes to the system disk, so the laserbridge account is
# granted exactly one privileged program - never a shell, never a wildcard.
# laserbridge-deploy validates its own arguments; see ADR 0005.
permit nopass laserbridge as root cmd /usr/sbin/laserbridge-deploy
