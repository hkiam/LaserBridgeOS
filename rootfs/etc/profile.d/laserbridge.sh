# shellcheck shell=sh
# Sourced by the login shell, so it carries no shebang of its own.
export EDITOR=vi
echo "LaserBridgeOS $(cat /etc/laserbridge/version 2>/dev/null) — immutable root; persistent state is under /data"
